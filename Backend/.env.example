# Copy this file to `.env` and fill in the real values.

# Google Gemini API key (Google AI Studio) used by the LLM structuring stage.
GEMINI_API_KEY=

# Postgres connection string (with pgvector enabled) used by the property
# database. Example: postgresql://user:password@host:5432/dbname
DATABASE_URL=

# Separate Postgres (Neon) connection string used by whatsappInquiryHandling
# for client records — a different database from DATABASE_URL above, so the
# two features' data never share a table or collide.
CLIENT_DATABASE_URL=

# Base URL the registration/update form link sent over WhatsApp is built
# from. Optional — leave blank to auto-detect this machine's LAN IP at
# startup (e.g. http://192.168.1.50:5173/whatsapp-inquiry), which is what a
# phone actually needs: a "localhost" link only opens from THIS machine's
# own browser, since "localhost" on the phone resolves to the phone itself.
# Set this explicitly only to point at a real deployed frontend instead of
# a LAN dev server.
INQUIRY_FORM_BASE_URL=

# Extra CORS-allowed origin matching the LAN IP above, e.g.
# http://192.168.1.50:5173 (no trailing slash). Optional — leave blank to
# auto-detect alongside INQUIRY_FORM_BASE_URL; set explicitly only when
# overriding that with a real deployed frontend origin.
FRONTEND_LAN_ORIGIN=

# How long the buffer waits for 10 qualified messages before flushing
# whatever it has anyway, in MINUTES. 60 = 1 hour. Optional — defaults to
# 60 if omitted. Lower this for fast local testing.
BATCH_WINDOW_MINUTES=60
