# Not a secret — just where the local backend runs (see Backend/main.py's
# `uvicorn main:app --reload --port 8000`). Override in `.env.local`
# (already gitignored via *.local) if your backend runs somewhere else.
VITE_API_BASE_URL=http://localhost:8000/api
